export enum packageManager {
    NPM = 'npm',
    YARN = 'yarn',
    PNPM = 'pnpm'
}

export enum tools {
    GIT = 'git',
    NODE = 'node',
    NPX = 'npx',
    VITE = 'vite',
    WEBPACK = 'webpack',
    PARCEL = 'parcel'
}